Django>=5.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
Pillow>=10.0
python-dotenv>=1.0

# Only needed if you set DB_ENGINE=mysql (see README "Using MySQL").
# Requires MySQL/MariaDB client dev headers to build - see README for the
# apt/brew package name on your OS. Comment out if staying on SQLite.
mysqlclient>=2.2
